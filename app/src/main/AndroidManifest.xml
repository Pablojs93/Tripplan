<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pjas.tripplan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".App.CreateTrip.MultiplePlaces.OnePlaceTrip"></activity>
        <activity
            android:name=".App.CreateTrip.MultiplePlaces.MultiplePlacesTrip"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".App.CreateTrip.SharedTrip.NoSharedTrip" />
        <activity android:name=".App.CreateTrip.SharedTrip.SharedTrip" />
        <activity android:name=".App.MyTrips.TripDetails" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDvz6yilc-vfRRlTgMUJMRRwYdl394fuzM" />

        <activity
            android:name=".App.MyTrips.MyTrips"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".App.CreateTrip.CreateTrip"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login.Register" />
        <activity android:name=".Login.Login" />
        <activity android:name=".Login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>